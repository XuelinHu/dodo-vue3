# PostgreSQL / PostGIS 配置（二选一）
#
# 方式 1：直接提供连接串（推荐，代码默认读取该值）
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dodo_vue3
#
# 方式 2：使用分项配置（未提供 DATABASE_URL 时会自动拼接）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dodo_vue3
DB_USER=postgres
DB_PASSWORD=postgres

# 服务配置
PORT=3000
NODE_ENV=development
